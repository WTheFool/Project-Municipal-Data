[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "municipal-core"
version = "0.1.0"
description = "Municipal analytics core engine"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]